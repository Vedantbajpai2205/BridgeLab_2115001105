using System;

class Distance
{
    static void Main(string[] args)
    {
        double kilometers = 10.8;
		double perKm = 1.6:
        double miles = kilometers * perKm;
		Console.WriteLine("The distance" + kilometers "km in miles is" + miles);
    }
}